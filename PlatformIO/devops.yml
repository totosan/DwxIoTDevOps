resources:
  containers:
  - container: pio-build
    image: 'totosan/iot_dev:latest'

pool: 'Hosted Ubuntu 1604'
container: pio-build

variables:
  buildConfiguration: 'Release'
  system.debug: true

steps:
- script: |
    ls -alH
    /opt/cmd.sh
  displayName: 'Build'